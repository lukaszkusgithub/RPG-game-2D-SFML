// Classes
#include "headers/Game.h"


int main()
{
//	int width = sf::VideoMode::getDesktopMode().width;
//	int height = sf::VideoMode::getDesktopMode().height;

//	sf::Vector2f characterSize(window.getSize().x / 20.0, window.getSize().y
//	                                                      / 20.0);
//	sf::Vector2f characterPosition(window.getSize().x / 2.0 - characterSize.x /
//	                                                          2.0,
//	                               window.getSize().y / 2.0 -
//	                               characterSize.y / 2.0);
//	Character character(characterSize, characterPosition);

	Game game;
	game.runGame();


	return 0;
}
